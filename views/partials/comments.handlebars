<p>Comments:</p><br>

<div class="row">
    {{#each this}}
    <div class="col-sm-2 text-center">
        <img src="../../assets/images/blogAva.png" class="img-circle" height="65" width="65" alt="Avatar">
    </div>
    <div class="col-sm-10">
        <h4> {{user.username}} <small> {{format_date created_at}} </small></h4>
        <p> {{comment_text}} </p>
        <br>
    </div>
    {{/each}}
</div>